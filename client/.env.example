# ★ Vite 用 VITE_ 前缀的变量才会被内联进 bundle

# 本地开发留空：client.ts 默认 '/api'，由 vite.config.ts 的 proxy 转发到本机 Express
VITE_API_BASE_URL=

# ★ Sealos 生产环境也是同源反代（baseURL='/api' → 前端 pod 的 server.js 把 /api/* 反代到后端）
#   所以 VITE_API_BASE_URL **不需要**设成后端公网 URL —— 设了反而走跨域，要后端 CORS 白名单放行。
#   真正的「后端地址」在 client/server.js 的 BACKEND_URL 默认值里（已修复为公网 URL）。