# ★ Vite 用 VITE_ 前缀的变量才会被内联进 bundle
# ★ 本地留空：client.ts 默认 '/api'，由 vite.config.ts 的 proxy 转发到 Express
# ★ 生产填 Railway 后端地址（含 /api 后缀）：
# VITE_API_BASE_URL=https://library-api.up.railway.app/api
VITE_API_BASE_URL=
